# Name of workflow
name: Launch tests

# Trigger the workflow on push or pull request
on:
  - push
  - pull_request

env:
  CI: true

jobs:
  launch ESLint:
    runs-on: ubuntu-latest

    steps:
      - run: make install
      - run: make lint