# Name of workflow
name: Launch tests

# Trigger the workflow on push or pull request
on:
  - push

env:
  CI: true

jobs:
  eslint:
    runs-on: ubuntu-latest

    steps:
      - run: make install
      - run: make lint